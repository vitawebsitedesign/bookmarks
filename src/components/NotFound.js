import React from 'react';
import BackButton from './BackButton';

const NotFound = () => (
    <>
        <nav>
            <BackButton />
        </nav>
        <p>404: Not found</p>;
    </>
);

export default NotFound;
